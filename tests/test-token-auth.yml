name: Test token auth with secrets
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Start Vault dev server
        run: |
          docker run -d \
            --name vault \
            --network host \
            -e VAULT_DEV_ROOT_TOKEN_ID=root \
            -e VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200 \
            hashicorp/vault:1.15 server -dev
          # Wait for Vault to be ready
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:8200/v1/sys/health > /dev/null 2>&1; then
              echo "Vault is ready"
              break
            fi
            echo "Waiting for Vault... ($i)"
            sleep 1
          done

      - name: Seed Vault with a secret
        run: |
          export VAULT_ADDR=http://127.0.0.1:8200
          export VAULT_TOKEN=root
          docker exec vault vault kv put secret/myapp username=testuser password=testpass

      - id: vault
        uses: .
        with:
          url: http://127.0.0.1:8200
          token: root
          secrets: secret/data/myapp username | MY_USERNAME ; secret/data/myapp password | MY_PASSWORD
          namespace: ""
          path: ""
          role: ""
          exportEnv: "true"
          exportToken: "false"
          outputToken: "true"
          tlsSkipVerify: "true"
          ignoreNotFound: "false"
          extraHeaders: |
            X-Custom-Header: test-value
          secretEncodingType: ""

      - name: Assert MY_USERNAME is set
        run: |
          echo "MY_USERNAME: $MY_USERNAME"
          if [ "$MY_USERNAME" != "testuser" ]; then
            echo "FAIL: expected MY_USERNAME=testuser, got '$MY_USERNAME'"
            exit 1
          fi
          echo "PASS"

      - name: Assert MY_PASSWORD is set
        run: |
          echo "MY_PASSWORD: $MY_PASSWORD"
          if [ "$MY_PASSWORD" != "testpass" ]; then
            echo "FAIL: expected MY_PASSWORD=testpass, got '$MY_PASSWORD'"
            exit 1
          fi
          echo "PASS"

      - name: Assert vault_token output is set
        env:
          VAULT_TOKEN_OUT: ${{ steps.vault.outputs.vault_token }}
        run: |
          echo "vault_token output: $VAULT_TOKEN_OUT"
          if [ -z "$VAULT_TOKEN_OUT" ]; then
            echo "FAIL: expected vault_token output to be non-empty"
            exit 1
          fi
          echo "PASS"
